<Window x:Class="EvacuateSimulation_MultiAgent.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EvacuateSimulation_MultiAgent"
        mc:Ignorable="d"
        Title="MainWindow" Height="635" Width="1000" Background="Black">
    <StackPanel Orientation="Horizontal">
        <!--レイアウトが表示されるコントロール-->
        <Image Height="600"
               Width="600"
               Source="{Binding Bitmap.Value}"
               Name="myImage" />

        <Button Content="Load"
                Margin="10,10,0,0"
                HorizontalAlignment="Left"
                Height="30"
                Width="100"
                Command="{Binding LoadLayoutCommand}" />
    </StackPanel>
</Window>
